; ============================================
; Dialplan Configuration
; Sistema de Monitoreo VoIP
; ============================================

[general]
static = yes
writeprotect = no
clearglobalvars = no

[globals]

[default]
exten => s,1,NoOp(Default context)
 same => n,Hangup()

; Dynamic dialplan generated by backend
#include extensions.monitoreo.conf
