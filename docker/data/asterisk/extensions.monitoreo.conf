
[monitoreo]
exten => s,1,Noop(Llamada Saliente)
same => n,playback(hello-world)
same => hangup()

exten => h,1,Set(RTCP_data=${CHANNEL(rtpqos,audio,all)})
exten => h,n,NoOp(RTCP Values : ${RTCP_data})

[entrantes]

exten => null,1,Noop(Llamada Entrante)
same => n,playback(hello-world)
same => hangup()

exten => null,1,Noop(Llamada Entrante)
same => n,playback(hello-world)
same => hangup()

exten => null,1,Noop(Llamada Entrante)
same => n,playback(hello-world)
same => hangup()

exten => 924171287,1,Noop(Llamada Entrante)
same => n,playback(hello-world)
same => hangup()

exten => null,1,Noop(Llamada Entrante)
same => n,playback(hello-world)
same => hangup()

exten => null,1,Noop(Llamada Entrante)
same => n,playback(hello-world)
same => hangup()

exten => null,1,Noop(Llamada Entrante)
same => n,playback(hello-world)
same => hangup()

exten => null,1,Noop(Llamada Entrante)
same => n,playback(hello-world)
same => hangup()

exten => null,1,Noop(Llamada Entrante)
same => n,playback(hello-world)
same => hangup()

exten => null,1,Noop(Llamada Entrante)
same => n,playback(hello-world)
same => hangup()

exten => null,1,Noop(Llamada Entrante)
same => n,playback(hello-world)
same => hangup()

exten => null,1,Noop(Llamada Entrante)
same => n,playback(hello-world)
same => hangup()

exten => 962382225,1,Noop(Llamada Entrante)
same => n,playback(hello-world)
same => hangup()

exten => 952354188,1,Noop(Llamada Entrante)
same => n,playback(hello-world)
same => hangup()

exten => 969055877,1,Noop(Llamada Entrante)
same => n,playback(hello-world)
same => hangup()

exten => 955465201,1,Noop(Llamada Entrante)
same => n,playback(hello-world)
same => hangup()

exten => 933988974,1,Noop(Llamada Entrante)
same => n,playback(hello-world)
same => hangup()

exten => null,1,Noop(Llamada Entrante)
same => n,playback(hello-world)
same => hangup()

exten => 929874066,1,Noop(Llamada Entrante)
same => n,playback(hello-world)
same => hangup()

exten => null,1,Noop(Llamada Entrante)
same => n,playback(hello-world)
same => hangup()

exten => null,1,Noop(Llamada Entrante)
same => n,playback(hello-world)
same => hangup()

exten => null,1,Noop(Llamada Entrante)
same => n,playback(hello-world)
same => hangup()

exten => null,1,Noop(Llamada Entrante)
same => n,playback(hello-world)
same => hangup()

exten => null,1,Noop(Llamada Entrante)
same => n,playback(hello-world)
same => hangup()

exten => 017194902,1,Noop(Llamada Entrante)
same => n,playback(hello-world)
same => hangup()

exten => 017194836,1,Noop(Llamada Entrante)
same => n,playback(hello-world)
same => hangup()
